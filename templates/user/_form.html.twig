{{ form_start(form) }}
    <div class="col-sm-9">
        {{ form_row(form.username) }}
    </div>
    <div class="col-sm-9">
        {{ form_row(form.password) }}
    </div>
    <div class="col-sm-9">
        {{ form_row(form.email) }}
    </div>
    <div class="col-sm-9">
        {{ form_row(form.roles) }}
    </div>
    <div class="col-sm-9">
        <br>
    </div>
    <div class="col-sm-9">
        {{ form_row(form.save) }}
    </div>
{{ form_end(form) }}


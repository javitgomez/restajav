{% macro print_row(dish) %}
    <tr>
        <td class="center">
            <div class="checkbox-table">
                <label>
                    <input type="checkbox" class="flat-grey selectall">
                </label>
            </div>
        </td>
        <td class="center"><img style="border:1px solid darkorange" width="64" src="{{ asset('uploads/dish_photo/' ~ dish.photo ) }}" alt="image"/></td>
        <td>{{ dish.photo }}</td>
        <td class="hidden-xs">{{ dish.name }}</td>
        <td class="hidden-xs">
            editar -
            añadir complemento -
            <span data-id-dish="{{ dish.id }}" class="del-action">eliminar</span>
        </td>
    </tr>
{% endmacro %}
{% macro print_row(category) %}
    <tr>
        <td class="center">
            <div class="checkbox-table">
                <label>
                    <input type="checkbox" class="flat-grey foocheck">
                </label>
            </div>
        </td>
        <td class="center">{{ category.name }}</td>
        <td>{{ category.description }}</td>
        <td class="hidden-xs">10</td>
        <td class="hidden-xs">
            <a href="{{ path('admin_category_edit', {'id': category.id }) }}" >editar</a>
            <a data-id-category="{{ category.id }}" class="add-action" href="javascript:void(0);">añadir platos</a>
            <a data-id-category="{{ category.id }}" class="del-action" href="javascript:void(0);">eliminar</a>
        </td>
    </tr>
{% endmacro %}
{{ form_start(form) }}
<div class="col-sm-9">
    {{ form_row(form.title) }}
</div>
<div class="col-sm-9">
    {{ form_row(form.description) }}
</div>
<div class="col-sm-9">
    {{ form_row(form.prize) }}
</div>
<div class="col-sm-9">
    {{ form_row(form.photo) }}
</div>
<div class="col-sm-9">
    <br>
</div>
<div class="col-sm-9">
    {{ form_row(form.save) }}
</div>
{{ form_end(form) }}
